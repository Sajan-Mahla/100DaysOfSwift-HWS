So today’s lesson was concept-based (no code written).

- Learned about `Text`
- Understood `Button` and how it works with `@State` (private)
- Studied `Form` usage
- Implemented `NavigationStack` (inline)
- Applied `.navigationTitle` for screen headers
- Explored `ForEach` loop for creating lists



PROJECT NAME IS WeSPLIT
MORE INFO SOON 💪🏻
